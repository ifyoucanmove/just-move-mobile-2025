import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChallengeWeek4Page } from './challenge-week4.page';

describe('ChallengeWeek4Page', () => {
  let component: ChallengeWeek4Page;
  let fixture: ComponentFixture<ChallengeWeek4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ChallengeWeek4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
