import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChallengeWeek3Page } from './challenge-week3.page';

describe('ChallengeWeek3Page', () => {
  let component: ChallengeWeek3Page;
  let fixture: ComponentFixture<ChallengeWeek3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ChallengeWeek3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
